fastapi>=0.111
uvicorn[standard]>=0.30
numpy>=1.26
scipy>=1.11
ezdxf>=1.2
pyproj>=3.6
Pillow>=10.0
httpx>=0.27
python-multipart>=0.0.9
pytest>=8.0
reportlab>=4.0
email-validator>=2.0
pytest-cov>=5.0
gpxpy>=1.6           # drive-test GPX ingestion (Phase 5 calibration)
# Optional: `mcp` enables the stdio MCP server (backend/mcp_server.py); the
# Copilot itself needs only httpx (above) + an AM_ANTHROPIC_API_KEY.
